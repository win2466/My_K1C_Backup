########################################
# Z-Offset with GuppyScreen on-screen tuning
# Sits outside Helper-Script/ so it can be edited freely.
#
# v2: Adds a suppression flag so that Creality's
# CX_* macros (which call SET_GCODE_OFFSET internally)
# cannot corrupt the persisted offset during START_PRINT.
#
# v3 (2026-09-25): No functional change. Confirmed this is the ONLY
# file defining [save_variables]; Helper-Script/save-zoffset.cfg is
# excluded in printer.cfg. Verify the filename below matches your
# actual variables.cfg location before uploading.
#
# HOW IT WORKS
# ------------
#   * SET_GCODE_OFFSET is redefined to intercept every call and persist
#     the Z value to variables.cfg.
#   * _ZOFFSET_FLAGS.suppress_save blocks that persistence while a
#     START_PRINT workflow is running, so Creality macros that call
#     SET_GCODE_OFFSET Z=0 internally cannot wipe the user's tuning.
#   * ZOFFSET_CLEAR zeroes the live gcode offset (MOVE=0) and blocks saves.
#   * ZOFFSET_RESTORE re-applies the saved value and unblocks saves.
#   * LOAD_GCODE_OFFSETS restores the saved value on Klipper startup.
########################################

# -----------------------------------------------------------------------------
# CHECK THIS PATH:
#   If your variables.cfg actually lives at
#       /usr/data/printer_data/config/variables.cfg
#   change the line below to that path. Otherwise leave it pointing at the
#   Helper-Script location (matches the original save-zoffset.cfg behaviour).
# -----------------------------------------------------------------------------
[save_variables]
filename: /usr/data/printer_data/config/Helper-Script/variables.cfg

[gcode_macro _ZOFFSET_FLAGS]
variable_suppress_save: 0
gcode:

# -----------------------------------------------------------------------------
# Intercepted SET_GCODE_OFFSET.
# Always forwards to the native Klipper command first, then persists Z
# only if the suppression flag is clear.
# -----------------------------------------------------------------------------
[gcode_macro SET_GCODE_OFFSET]
description: Saving Z-Offset (guarded against Creality macro resets)
rename_existing: _SET_GCODE_OFFSET
gcode:
  {% if printer.save_variables.variables.zoffset %}
    {% set zoffset = printer.save_variables.variables.zoffset %}
  {% else %}
    {% set zoffset = {'z': None} %}
  {% endif %}
  {% set ns = namespace(zoffset={'z': zoffset.z}) %}

  ; Always forward to the real Klipper command first
  _SET_GCODE_OFFSET {% for p in params %}{'%s=%s '% (p, params[p])}{% endfor %}

  ; Only persist if we are NOT inside a START_PRINT workflow
  {% if printer['gcode_macro _ZOFFSET_FLAGS'].suppress_save|int == 0 %}
    {% if 'Z' in params %}
      {% set null = ns.zoffset.update({'z': params.Z}) %}
    {% endif %}
    {% if 'Z_ADJUST' in params %}
      {% if ns.zoffset.z == None %}
        {% set null = ns.zoffset.update({'z': 0}) %}
      {% endif %}
      {% set null = ns.zoffset.update({'z': (ns.zoffset.z | float) + (params.Z_ADJUST | float)}) %}
    {% endif %}
    SAVE_VARIABLE VARIABLE=zoffset VALUE="{ns.zoffset}"
  {% endif %}

# -----------------------------------------------------------------------------
# Called by START_PRINT before homing/mesh.
# Zeroes the live offset and blocks any accidental save.
# -----------------------------------------------------------------------------
[gcode_macro ZOFFSET_CLEAR]
description: Temporarily zero the gcode Z-offset and block saves until restore
gcode:
  SET_GCODE_VARIABLE MACRO=_ZOFFSET_FLAGS VARIABLE=suppress_save VALUE=1
  _SET_GCODE_OFFSET Z=0 MOVE=0
  RESPOND TYPE=command MSG="Z-offset suspended for mesh calibration (saves blocked)"

# -----------------------------------------------------------------------------
# Called by START_PRINT after mesh, before the purge line.
# Re-applies the saved value and re-enables persistence.
# -----------------------------------------------------------------------------
[gcode_macro ZOFFSET_RESTORE]
description: Re-apply the saved gcode Z-offset and re-enable saves
gcode:
  {% if printer.save_variables.variables.zoffset %}
    {% set zoffset = printer.save_variables.variables.zoffset %}
    {% if zoffset.z %}
      _SET_GCODE_OFFSET Z={zoffset.z} MOVE=0
      RESPOND TYPE=command MSG="Z-offset restored: {zoffset.z}mm"
    {% else %}
      RESPOND TYPE=command MSG="No stored Z-offset to restore"
    {% endif %}
  {% else %}
    RESPOND TYPE=command MSG="variables.cfg missing zoffset entry"
  {% endif %}
  SET_GCODE_VARIABLE MACRO=_ZOFFSET_FLAGS VARIABLE=suppress_save VALUE=0

# -----------------------------------------------------------------------------
# Manual re-apply (usable from console or GuppyScreen).
# -----------------------------------------------------------------------------
[gcode_macro ZOFFSET_APPLY]
description: Apply the saved gcode Z-offset now
gcode:
  ZOFFSET_RESTORE

# -----------------------------------------------------------------------------
# Restore on Klipper start.
# -----------------------------------------------------------------------------
[delayed_gcode LOAD_GCODE_OFFSETS]
initial_duration: 2
gcode:
  ZOFFSET_RESTORE